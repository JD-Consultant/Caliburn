"""Per-section OCS extractors (extracted from the OCSTransformer god-file, Phase 3b).

Each module owns one PDF section: version history, profile, content (OCU
units), attitude, and notes. The transformer orchestrates them.
"""
