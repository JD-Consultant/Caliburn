# CT-06：工作相關資訊取捨與正常訪談

2026-09-07 · LLM-Q019 · isolated G5/G7 · Owner 已准局部優化及新帳本24次 Luna／medium、US$0.10，包含背景與重試。

## 目的、依據與界線

依[已核准資訊取捨研究](../../../../docs/specs/2026-09-07-work-case-and-understanding-information-selection.md)§3–4：案例保留職務分析需要的實際細節；理解歸納共同工作並保留重要差異，不能用本批資料取代全部舊理解。無關瑣事可省，低頻／責任界線不可因此丟掉。沿用三欄抽取、既有工具、Store／Saver、推理延續及ABC；不加欄位、代理、評分器或JD功能。

本輪再核對[OpenAI GPT-5.6 prompting guidance](https://developers.openai.com/api/docs/guides/prompt-guidance-gpt-5p6)：說清成果、事實保留與完成條件，以實際 traces 做局部調整，不整套重寫。其餘 OpenAI／Anthropic／職務分析來源由上方研究持有，不複製長文。

## 執行順序

- [x] 1. 回讀研究、實際提示與 CT05 證據，跑既有離線回歸；先檢查上次B2恢復點，不重跑舊整套。
- [x] 2. 校準 `extraction.py` 的現有提示及三欄說明；`consolidation.py` 與共用 `memory_tools.py` 對齊工作相關資訊取捨。顧問入口 `api.py` 僅補有證據的訪談表達／追問缺口；分析Skill若已符合不改。
- [x] 3. 新帳本小額核對真實抽取與正常API訪談、背景發布、獨立Memory回查。每步保存結果，所有模型請求共同計費。品質用實際內容判讀，不以prompt字串斷言代替；不重建大型eval平台。
- [x] 4. 完整既有回歸（含專用PG）、diff與獨立review；短結果文檔連結分離證據，更新根register。只保存本輪檔案／本地tag，不merge/push。

## 驗收與停止線

需看到：實際做法／責任／頻率／重要例外能找回；一般工作不隨案例數重複膨脹；局部補充未洗掉其他有效工作；不把一次日期／耗時推成慣例，不把AI問題變成員工事實。尚未回答的事情不猜，正常訪談不機械重問已知資訊。

先不承諾所有領域／任意長度都正確。若小測仍有語意問題或費用／次數到界線，記錄具體輸出與下一個問題，不無限堆提示或自行新增機制。遇需改架構／發布政策的新問題先討論。

## 本輪進度

初始診斷：B1對「不丟細節」規則未清楚界定職務相關性；B2與共用編輯指引也需要一致的取捨語意。原三份按需Skill已含工作範圍／案例比較／專業判斷方向，先保留。既有CT05發布待驗尚未關閉。

實測接續：[CT-06結果](../specs/2026-09-07-relevant-work-memory-calibration.md)。24次／估US$0.02428753已停止；CT05已發布，D案例及獨立回查通過，但背景操作效率OPEN。實測發現顯示行號後空白誤帶入精確修改，核對官方底層後補提示；不改匹配機制。診斷的12步不是產品上限翻案。空白指引微測未走到編輯，保留未驗狀態。下一輪只處理CT06-Q01，不再重跑整組。
