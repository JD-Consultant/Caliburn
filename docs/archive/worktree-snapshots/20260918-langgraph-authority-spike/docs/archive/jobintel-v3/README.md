# Archive — jobintel-ai v3（歷史記錄）

這裡是 **Caliburn monorepo 成形前**、`jobintel-ai` 在 v3 時代（2026-06-14～06-27）的設計、決策與計畫文件,合併進 monorepo（Phase 1）時一併歸檔保留。

> **這些是歷史快照,不再現行維護。** 現行的系統架構以 [`../../specs/2026-06-27-system-architecture-design.md`](../../specs/2026-06-27-system-architecture-design.md) 為準;若與此處舊文件衝突,以現行系統文檔為準。

## 內容

- `specs/` —— v3 時代各設計/研究/決策（jd-authoring-flow、v3-architecture、refactor-decision-log、ksa-flow-redesign、llm-agent、field-provenance…等）。
- `plans/` —— v3 時代各階段實作計畫（phase1-data-layer ～ phase5-otel-eval、db-document-centric、api-v4-migration…等）。
- `PROJECT-STATUS.md` —— v3 跨 session 狀態快照（已被 monorepo + 記憶取代）。
- `jobintel-docs-index.md` —— 舊的 jobintel 文件索引。

git 完整歷史(含這些檔案的演進)已透過 `git subtree` 保留在 monorepo 中。
