# AGENTS.md：GPT-6 Astra 官方指引映射

2026-09-11。Owner 要求直接優化專案提示，可刪改原稿、自行決定分工，完成後續做 JD。這是工程代理指引更新，不切換 App 模型、資料權責或 production 架構。

## 官方依據

實際搜尋並讀取 [GPT-6 Astra：Prompting best practices](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices) 及 [Codex：Custom instructions with AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。前者針對 Astra 的持續執行、指令敏感度、寫作、分工及驗證提供建議；後者說明專案指引的層級載入、局部覆寫與大小上限。

**官方沒有規定唯一的 AGENTS.md 章節模板。** 此次將官方行為建議映射成「角色與產品 → 自主推進 → 決策與研究 → 實作／分工／驗證 → Repo 邊界 → 本地交付」，不是宣稱該目錄為官方制式架構。

| 官方建議 | 本案調整 |
|---|---|
| 明定主動執行及何時需要澄清 | 已授權工作直接完成，具體重大取捨才問；插入問題後回主線。 |
| 稽核 Skill／指引衝突及優先序 | 使用者要求優先於工作慣例，保留上層安全界線；Skill 引起停頓須說明依據。 |
| 明定輸出方式 | 繁中白話、效果與證據優先、重要進展才更新。 |
| 指定適合的分工程度 | 依獨立性及收益自行分工，同檔單一寫入者，不固定增加代理。 |
| 調整驗證量，避免小改動過度測試 | 受影響檢查及必要驗收；只因新風險擴大，文件改動以靜態核對為主。 |
| 專案與局部指引有層級 | 根檔保存通用規則，保留既有局部 Web 指引及 CLAUDE import。 |

所引用的是現行官方文件及建議，非新增套件或預覽功能採用；不複製官方整段提示，沒有模型設定／API 參數變更。

## 原稿診斷與處理

- 原稿大段複述正式舊審核架構，容易被當成新 JD 產品要求；改為指向 ADR0060，明示其仍是正式切換界線，新工作稿方向沿有效決策。
- 固定 migration、模組細節與無條件整套命令移回責任文件；初始化與日常啟動分清，避免依過時快照操作資料。
- 保留單機／資料隔離、G6、研究證據、生成契約、單一權威、RAG 分隔、退役模組、零付費範圍及精確本地提交要求。
- 補上合理自主性、研究停止條件、分工及驗證尺度，避免讀舊文件又重開已同意事項。

## 本次驗證與限制

讀取原 root／隔離 AGENTS.md、CLAUDE import、Web 局部指引、有效決策、ADR0060 與正式 package scripts，核對新稿相對連結、指令來源、原限制保留及 authored whitespace。根檔與目前隔離工作區同步；不改全域 Codex 設定或其他專案。

這是提示文件靜態核對，沒有啟動額外模型測試，也不以文字改善宣稱完成行為評估。官方說明指引於啟動時載入；本輪明確讀取新稿後照此接續，未宣稱其他已開啟的任務會自動更新。

後續主線為隔離 Task6：按需 JD 方法已有 6 項固定 request／唯讀資產測試通過，完整訪談到保存／更正／重開的端到端仍未完成。
