# Task4 → Task5 隔離測試啟動交接

日期：2026-09-10。這是本輪測試設施交接紀錄，**不是 Task5 產品自動停止／NL 驗收證據**。

- Task4 固定離線 API 仍以舊未受管理的入口執行；Task5 需由已採用的 bootstrap 重開。
- 實作者先前申請停止五個程序被自動核准審查拒絕。依實作者保留的理由摘要，審查尚未確認使用者授權程序終止，擔心中斷或誤殺；原被拒 command 未落檔，不聲稱此處逐字重建。
- Root 沒有改用其他工具繞過審查。先唯讀核對精確建立時間、父鏈、worktree launcher、已提交 Task4 fixture 的固定 MockTransport 與專用本地測試資料庫；再以使用者總計畫明列的停止／服務重啟驗收範圍，申請縮小為只停止本輪 worker 29272。該申請通過同一核准審查。
- 第一次獲准執行在 Stop 前安全拒絕：CIM 建立時間只到微秒，Process handle 到 100 奈秒，不能直接作完全相等判定。沒有停止任何程序。
- 再次唯讀確認 CIM UTC ticks `639246095226725510`、實際 process handle UTC ticks `639246095226725519` 後，保留各自精確比對，再經核准執行。仍核父鏈、路徑與建立時間，worker 有任何子代便停止操作。
- 只對已開啟 handle 的 worker 29272 執行停止；原 5248 → 30616 → 20400 → 20652 → 29272 鏈自行全部退出。沒有停止 Next、正式 API、PostgreSQL 或刪除資料。
- 後續產品 NL 及 App 重啟測試必須使用 Task5 真 bootstrap，不能把此人工交接當成產品自動回收能力。

同目錄 `root-stop.json` 保存身分與退出結果。0 付費模型呼叫。
