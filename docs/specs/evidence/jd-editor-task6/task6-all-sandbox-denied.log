...EEEEEEEEEEEEEEEEEEEEEEE.E.....EEEEEE................................. [  9%]
........................................................................ [ 18%]
....EEEEEEEEEEEE........................................................ [ 28%]
.....................................................................EEE [ 37%]
EEEEE..EE.sssssssEEEsssssssssss........sssssssssEsssssEEssssssss.sssssss [ 46%]
ssss.ss....sssssssssssssssssssssssssssssssssssssss.s.EE.....ssEE........ [ 56%]
........sssssssssss......................................EE............. [ 65%]
.....................................................................E.. [ 74%]
...........E........sssssssssssssssssssssssEEEEEEEEEEEEEEEEss........... [ 84%]
.....................................EEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE [ 93%]
EEEEEEEEEEEEEEE.EEEEEEEEEE...............F..FF....F                      [100%]
=================================== ERRORS ====================================
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[work-scope-interview-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[work-scope-interview-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[compare-work-patterns-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[compare-work-patterns-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[outcomes-and-expertise-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_first_input_advertises_metadata_then_reads_only_selected_body[outcomes-and-expertise-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_skill_read_preserves_inline_compaction_pairing_and_canonical_prefix _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/../service.py-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/../service.py-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/../../../../README.md-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/../../../../README.md-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills\\..\\service.py-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills\\..\\service.py-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/.env-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/.env-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/README.md-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/README.md-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agent\\tests\\test_analysis_skills.py-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agent\\tests\\test_analysis_skills.py-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/S:/caliburn/.worktrees/analysis-only-agent/experiments/analysis-agent/tests/test_analysis_skills.py-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_file_rejects_traversal_and_host_paths[/skills/S:/caliburn/.worktrees/analysis-only-agent/experiments/analysis-agent/tests/test_analysis_skills.py-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_invalid_routed_ls_directory_is_tool_feedback_not_service_interruption[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_invalid_routed_ls_directory_is_tool_feedback_not_service_interruption[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_shared_ls_grep_and_read_use_the_same_advertised_mount _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_skill_read_uses_existing_trusted_read_recovery[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_skill_read_uses_existing_trusted_read_recovery[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_skill_tool_body_stays_canonical_but_never_enters_b1_source_or_wire _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_lifespan_idempotency_and_private_state_projection _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
______ ERROR at setup of test_api_cross_document_access_and_empty_input _______

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_background_status_is_safe_and_not_a_manual_consolidation_entry _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_a_each_call_includes_skills_memory_guide_results_and_preserves_canonical[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_a_each_call_includes_skills_memory_guide_results_and_preserves_canonical[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_a_budget_error_closes_unlocks_and_is_not_resumable[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_a_budget_error_closes_unlocks_and_is_not_resumable[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
___ ERROR at setup of test_b1_schema_and_b2_read_loop_actual_payload[False] ___

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
___ ERROR at setup of test_b1_schema_and_b2_read_loop_actual_payload[True] ____

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_background_budget_blocks_without_repeat_per_tick[b1] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_background_budget_blocks_without_repeat_per_tick[b2] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_surfaces_safe_counter_failure_without_generation[overflow-context_budget_exceeded-configuration_error-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_surfaces_safe_counter_failure_without_generation[schema-configuration_error-configuration_error-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_surfaces_safe_counter_failure_without_generation[utf8-configuration_error-configuration_error-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_api_surfaces_safe_counter_failure_without_generation[timeout-transport_error-interrupted-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_background_selects_high_only_for_extraction[1000-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_background_selects_high_only_for_extraction[1000-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_background_selects_high_only_for_extraction[6000-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_background_selects_high_only_for_extraction[6000-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_real_factory_routes_models_through_same_budget_and_lifetime[None-None] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_real_factory_routes_models_through_same_budget_and_lifetime[high-None] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_real_factory_routes_models_through_same_budget_and_lifetime[medium-high] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_real_factory_routes_models_through_same_budget_and_lifetime[medium-medium] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_root_only_manual_binding_preserves_messages_and_first_true_input[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_root_only_manual_binding_preserves_messages_and_first_true_input[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_create_identity_metadata_and_ui_filter_do_not_filter_scheduler _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
__________ ERROR at setup of test_two_web_origins_and_preflight_only __________

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_run_lookup_is_read_only_and_digest_covers_abandon_pending _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_r01_archive_between_admission_sections_cannot_save_input _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_r04_foreground_closure_does_not_hold_other_document[stop] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_r04_foreground_closure_does_not_hold_other_document[abandon] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_sdk_discovers_method_then_reads_only_requested_asset[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_sdk_discovers_method_then_reads_only_requested_asset[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_____ ERROR at setup of test_close_waits_for_stop_final_projection[False] _____

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_____ ERROR at setup of test_close_waits_for_stop_final_projection[True] ______

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_initial_memory_request_matches_reviewed_contract _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_default_budget_can_complete_multisource_read _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_provider_overflow_closes_turn_without_erasing_source_or_locking_next_input _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_native_api_two_turns_replay_reasoning_and_report_actual_usage _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_background_rebuild_resumes_exact_saved_stage[notification_only-2] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_background_rebuild_resumes_exact_saved_stage[b1_saved-1] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_background_rebuild_resumes_exact_saved_stage[b2_tool_saved-1] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_background_rebuild_resumes_exact_saved_stage[memory_prepared-0] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_background_rebuild_resumes_exact_saved_stage[publication_reply-0] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_blocked_error_remains_blocked_after_rebuilding_all_clients _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[saved_input] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[saved_result] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[transport_failure] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_main_service_reads_document_memory_and_keeps_native_items _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_service_rebuild_validates_and_reads_memory_source_links _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_uncertain_repair_can_explicitly_resume_same_operation[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_uncertain_repair_can_explicitly_resume_same_operation[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[None] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[native] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[exact] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[read_conversation-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[read_conversation-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[read_file-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[read_file-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[ls-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[ls-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[grep-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_trusted_read_resumes_original_call_without_prior_replay[grep-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_resume_at_model_limit_finishes_tool_without_resetting_budget[False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_resume_at_model_limit_finishes_tool_without_resetting_budget[True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_closure_pairs_unavailable_result_and_allows_new_input[stop-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_closure_pairs_unavailable_result_and_allows_new_input[stop-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_closure_pairs_unavailable_result_and_allows_new_input[abandon-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_read_closure_pairs_unavailable_result_and_allows_new_input[abandon-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unknown_entered_tool_stays_blocked[runtime_error-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unknown_entered_tool_stays_blocked[runtime_error-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unknown_entered_tool_stays_blocked[process_interrupted-False] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unknown_entered_tool_stays_blocked[process_interrupted-True] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_same_name_tool_cannot_inherit_memory_read_capability[read_conversation] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_same_name_tool_cannot_inherit_memory_read_capability[read_file] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_same_name_tool_cannot_inherit_memory_read_capability[ls] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_same_name_tool_cannot_inherit_memory_read_capability[grep] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_notification_runs_real_b_without_repeating_published_source _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_background_wire_preserves_configured_output_limit[1000] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_background_wire_preserves_configured_output_limit[6000] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unnotified_text_stays_saved_until_explicit_fallback_threshold _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_failed_b_stays_blocked_across_ticks_new_input_and_rebuild _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
____ ERROR at setup of test_b_is_independent_and_only_one_can_run_globally ____

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_partial_batch_keeps_target_after_its_first_notification_is_consumed _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_rebuild_resumes_saved_b1_without_another_extraction __

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
__ ERROR at setup of test_repeated_process_loss_cannot_reset_recovery_budget __

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_commit_reply_loss_reconciles_without_model_or_stale_failure_notice _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_current_failure_is_request_context_not_conversation_and_clears_on_recovery _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_scheduler_wakes_real_background_and_service_close_joins_it _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
___ ERROR at setup of test_blocked_document_does_not_starve_other_documents ___

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_pending_foreground_is_not_extracted_and_closed_failure_is_not_lost _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_dispatcher_cannot_start_model_after_service_shutdown _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_accepted_input_is_saved_and_double_submit_does_not_invoke_twice _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_service_default_does_not_cut_off_multistep_work_before_final _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_stop_waits_for_provider_and_does_not_execute_returned_tool _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_reopen_lost_api_reply_reconciles_checkpoint_without_paid_restart _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_transient_failure_resumes_same_input_and_persists_resume_count _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_configuration_failure_seals_turn_and_new_input_is_allowed _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_failed_input_save_can_retry_same_key_without_being_stuck _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_saved_input_without_worker_is_recovered_not_appended_twice _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_explicit_abandon_preserves_original_employee_message _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_shutdown_joins_inflight_provider_before_client_can_close _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_stop_saved_input_before_child_exists_and_restart_remembers_stop _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_completed_usage_is_reported_from_real_provider_metadata _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_stop_after_transport_failure_does_not_offer_unwanted_resume _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unsaved_old_request_cannot_acknowledge_or_stop_another_live_run[repeat] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_unsaved_old_request_cannot_acknowledge_or_stop_another_live_run[stop] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_exhausted_tool_quota_still_allows_resuming_final_model_answer _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
______ ERROR at setup of test_stop_after_model_hooks_prevents_tool_entry ______

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[credit_balance_exhausted] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[organization_spend_limit_exceeded] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[project_spend_limit_exceeded] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[organization_usage_limit_exceeded] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
_ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[insufficient_quota] _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
___ ERROR at setup of test_billing_errors_do_not_offer_manual_resume[None] ____

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
>           os.rmdir(path)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:637: PermissionError

During handling of the above exception, another exception occurred:

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
ignore_errors = False, onerror = None
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))
dir_fd = None

    def rmtree(path, ignore_errors=False, onerror=None, *, onexc=None, dir_fd=None):
        """Recursively delete a directory tree.
    
        If dir_fd is not None, it should be a file descriptor open to a directory;
        path will then be relative to that directory.
        dir_fd may not be implemented on your platform.
        If it is unavailable, using it will raise a NotImplementedError.
    
        If ignore_errors is set, errors are ignored; otherwise, if onexc or
        onerror is set, it is called to handle the error with arguments (func,
        path, exc_info) where func is platform and implementation dependent;
        path is the argument to that function that caused it to fail; and
        the value of exc_info describes the exception. For onexc it is the
        exception instance, and for onerror it is a tuple as returned by
        sys.exc_info().  If ignore_errors is false and both onexc and
        onerror are None, the exception is reraised.
    
        onerror is deprecated and only remains for backwards compatibility.
        If both onerror and onexc are set, onerror is ignored and onexc is used.
        """
    
        sys.audit("shutil.rmtree", path, dir_fd)
        if ignore_errors:
            def onexc(*args):
                pass
        elif onerror is None and onexc is None:
            def onexc(*args):
                raise
        elif onexc is None:
            if onerror is None:
                def onexc(*args):
                    raise
            else:
                # delegate to onerror
                def onexc(*args):
                    func, path, exc = args
                    if exc is None:
                        exc_info = None, None, None
                    else:
                        exc_info = type(exc), exc, exc.__traceback__
                    return onerror(func, path, exc_info)
    
        if _use_fd_functions:
            # While the unsafe rmtree works fine on bytes, the fd based does not.
            if isinstance(path, bytes):
                path = os.fsdecode(path)
            stack = [(os.lstat, dir_fd, path, None)]
            try:
                while stack:
                    _rmtree_safe_fd(stack, onexc)
            finally:
                # Close any file descriptors still on the stack.
                while stack:
                    func, fd, path, entry = stack.pop()
                    if func is not os.close:
                        continue
                    try:
                        os.close(fd)
                    except OSError as err:
                        onexc(os.close, path, err)
        else:
            if dir_fd is not None:
                raise NotImplementedError("dir_fd unavailable on this platform")
            try:
                if _rmtree_islink(path):
                    # symlinks to directories are forbidden, see bug #1669
                    raise OSError("Cannot call rmtree on a symbolic link")
            except OSError as err:
                onexc(os.path.islink, path, err)
                # can't continue even if onexc hook returns
                return
>           return _rmtree_unsafe(path, onexc)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:781: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

path = '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
onexc = functools.partial(<function on_rm_rf_error at 0x000002E1E6B77100>, start_path=WindowsPath('//?/S:/caliburn/.worktrees/analysis-only-agent/scratch/task6-final-all-temp'))

    def _rmtree_unsafe(path, onexc):
        def onerror(err):
            onexc(os.scandir, err.filename, err)
        results = os.walk(path, topdown=False, onerror=onerror, followlinks=os._walk_symlinks_as_files)
        for dirpath, dirnames, filenames in results:
            for name in dirnames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.rmdir(fullname)
                except OSError as err:
                    onexc(os.rmdir, fullname, err)
            for name in filenames:
                fullname = os.path.join(dirpath, name)
                try:
                    os.unlink(fullname)
                except OSError as err:
                    onexc(os.unlink, fullname, err)
        try:
            os.rmdir(path)
        except OSError as err:
>           onexc(os.rmdir, path, err)
E           PermissionError: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'

C:\Users\chenb\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\Lib\shutil.py:639: PermissionError
================================== FAILURES ===================================
______ test_actual_bootstrap_mutex_competitor_cannot_terminate_live_api _______

    def test_actual_bootstrap_mutex_competitor_cannot_terminate_live_api():
        assert importlib.util.find_spec('analysis_agent.windows_lifecycle'), 'App bootstrap is absent'
        key = 'test-' + uuid4().hex
        first = launch(key)
        try:
            ready = json.loads(first.stdout.readline())
            assert ready['ready'] and ready['member'] and not ready['handle_inheritable']
            second = launch(key)
            output, error = second.communicate(timeout=10)
>           assert second.returncode != 0 and 'already_running' in error
E           assert (1 != 0 and 'already_running' in 'Traceback (most recent call last):\n  File "S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agen...      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\npywintypes.error: (5, \'CreateMutex\', \'存取被拒。\')\n')
E            +  where 1 = <Popen: returncode: 1 args: ['S:\\caliburn\\.worktrees\\analysis-only-agent\...>.returncode

tests\test_windows_lifecycle.py:32: AssertionError
___ test_abandoned_main_thread_still_requires_product_to_stop_exact_old_job ___

    def test_abandoned_main_thread_still_requires_product_to_stop_exact_old_job():
        key = 'test-'+uuid4().hex
        first = launch(key)
        ready = json.loads(first.stdout.readline())
        first.stdin.write('abandon\n'); first.stdin.flush()
        second = launch(key)
        output,error = second.communicate(timeout=10)
>       assert second.returncode != 0 and 'prior_job_object_still_exists' in error
E       assert (1 != 0 and 'prior_job_object_still_exists' in 'Traceback (most recent call last):\n  File "S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agen...      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\npywintypes.error: (5, \'CreateMutex\', \'存取被拒。\')\n')
E        +  where 1 = <Popen: returncode: 1 args: ['S:\\caliburn\\.worktrees\\analysis-only-agent\...>.returncode

tests\test_windows_lifecycle.py:75: AssertionError
___ test_external_old_job_handle_prevents_reuse_until_a_new_explicit_start ____

    def test_external_old_job_handle_prevents_reuse_until_a_new_explicit_start():
        import win32job
        key = 'test-'+uuid4().hex
        first = launch(key)
        assert json.loads(first.stdout.readline())['ready']
        external = win32job.OpenJobObject(win32job.JOB_OBJECT_QUERY,False,'Local\\Caliburn.'+key+'.ApiJob')
        try:
            first.stdin.write('crash\n'); first.stdin.flush(); first.wait(timeout=5)
            second = launch(key)
            output,error = second.communicate(timeout=10)
>           assert second.returncode != 0 and 'prior_job_object_still_exists' in error
E           assert (1 != 0 and 'prior_job_object_still_exists' in 'Traceback (most recent call last):\n  File "S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agen...cleError(\'prior_job_open_failed\') from exc\nanalysis_agent.windows_lifecycle.LifecycleError: prior_job_open_failed\n')
E            +  where 1 = <Popen: returncode: 1 args: ['S:\\caliburn\\.worktrees\\analysis-only-agent\...>.returncode

tests\test_windows_lifecycle.py:100: AssertionError
___ test_new_bootstrap_crash_during_old_job_stop_never_claims_app_admission ___

    def test_new_bootstrap_crash_during_old_job_stop_never_claims_app_admission():
        key='test-'+uuid4().hex
        first=launch(key)
        assert json.loads(first.stdout.readline())['ready']
        first.stdin.write('abandon\n');first.stdin.flush()
        second=launch(key,'old_stop_crash')
        output,error=second.communicate(timeout=10)
>       assert second.returncode==91 and json.loads(output)=={'fault':'old_job_termination_sent','app_work':False}
E       AssertionError: assert (1 == 91)
E        +  where 1 = <Popen: returncode: 1 args: ['S:\\caliburn\\.worktrees\\analysis-only-agent\...>.returncode

tests\test_windows_lifecycle.py:148: AssertionError
============================== warnings summary ===============================
.venv\Lib\site-packages\starlette\testclient.py:53
  S:\caliburn\.worktrees\analysis-only-agent\experiments\analysis-agent\.venv\Lib\site-packages\starlette\testclient.py:53: DeprecationWarning: The anyio.abc.BlockingPortal alias is deprecated, use anyio.from_thread.BlockingPortal instead.
    _PortalFactoryType = Callable[[], AbstractContextManager[anyio.abc.BlockingPortal]]

tests/test_analysis_skills.py: 27 warnings
tests/test_api.py: 3 warnings
tests/test_context_budget.py: 12 warnings
tests/test_extraction_role.py: 8 warnings
tests/test_jd_admission.py: 2 warnings
tests/test_jd_api.py: 3 warnings
tests/test_jd_fix1_admission.py: 3 warnings
tests/test_jd_skill_contract.py: 2 warnings
tests/test_jd_stop_projection.py: 2 warnings
tests/test_memory_prompt_contract.py: 2 warnings
tests/test_native_context_budget.py: 2 warnings
tests/test_postgres_scheduling.py: 6 warnings
tests/test_postgres_service.py: 10 warnings
tests/test_read_recovery.py: 22 warnings
tests/test_scheduling.py: 15 warnings
tests/test_service.py: 23 warnings
  S:\caliburn\.worktrees\analysis-only-agent\experiments\analysis-agent\.venv\Lib\site-packages\_pytest\pathlib.py:102: PytestWarning: (rm_rf) unknown function <built-in function scandir> when removing \\?\S:\caliburn\.worktrees\analysis-only-agent\scratch\task6-final-all-temp:
  <class 'PermissionError'>: [WinError 5] 存取被拒。: '\\\\?\\S:\\caliburn\\.worktrees\\analysis-only-agent\\scratch\\task6-final-all-temp'
    warnings.warn(

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ===========================
SKIPPED [1] tests\test_jd_admission.py:62: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_admission.py:105: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_admission.py:147: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [4] tests\test_jd_admission.py:181: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_api.py:83: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_api.py:136: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_api.py:166: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_close_reconcile.py:14: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_current_source_acquisition.py:26: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_end_to_end.py:58: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_end_to_end.py:140: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_fix1.py:14: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_fix1.py:38: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_fix1.py:74: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_fix1.py:89: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_fix1.py:136: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_fix1_admission.py:29: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_fix1_admission.py:52: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_fix1_admission.py:81: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_fix1_admission.py:107: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_fix2.py:12: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_manual_recovery.py:35: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_manual_recovery.py:58: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_manual_recovery.py:75: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_manual_recovery.py:105: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_manual_recovery.py:133: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:9: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:53: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:74: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:102: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:120: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:141: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:155: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:174: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:186: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:214: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:229: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view.py:246: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view_recovery.py:13: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_model_view_recovery.py:63: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:58: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:70: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:102: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:120: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_postgres.py:146: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:163: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:175: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:188: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:212: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:227: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:250: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:273: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:290: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:305: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:323: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:339: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:385: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:397: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:417: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:435: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:452: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres.py:469: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_postgres_recovery.py:9: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_postgres_recovery.py:57: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_postgres_recovery.py:70: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [5] tests\test_jd_process_lifecycle.py:28: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_process_lifecycle.py:50: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [2] tests\test_jd_process_lifecycle.py:71: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_provider_binding.py:12: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_provider_binding.py:67: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_sources.py:8: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_sources.py:37: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:23: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:105: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:116: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:132: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_jd_tools.py:143: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:156: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:168: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:197: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [1] tests\test_jd_tools.py:216: Dedicated JD PostgreSQL DSN required; not a persistence pass
SKIPPED [3] tests\test_postgres_consolidation.py:30: Dedicated q019_agent_test required; not a durability pass
SKIPPED [3] tests\test_postgres_consolidation_request.py:21: Dedicated q019_agent_test required; notification durability NOT verified
SKIPPED [1] tests\test_postgres_conversation.py:18: Set Q019_TEST_DATABASE_URL to dedicated q019_agent_test DB; not a durability pass
SKIPPED [4] tests\test_postgres_conversation_lifecycle.py:31: Dedicated q019_agent_test required; root/child durability NOT verified
SKIPPED [2] tests\test_postgres_extraction.py:24: Requires dedicated q019_agent_test; not a durability pass
SKIPPED [4] tests\test_postgres_live_memory.py:30: Dedicated q019_agent_test required; not a durability pass
SKIPPED [1] tests\test_postgres_memory.py:22: Set Q019_TEST_DATABASE_URL to dedicated q019_agent_test DB; not a persistence pass
SKIPPED [4] tests\test_postgres_publication.py:57: Dedicated q019_agent_test DSN required; not a concurrency pass
SKIPPED [1] tests\test_postgres_publication.py:97: Dedicated q019_agent_test DSN required; not a concurrency pass
SKIPPED [2] tests\test_postgres_summary_reextraction.py:31: Dedicated q019_agent_test required; not a durability pass
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[work-scope-interview-False]
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[work-scope-interview-True]
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[compare-work-patterns-False]
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[compare-work-patterns-True]
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[outcomes-and-expertise-False]
ERROR tests/test_analysis_skills.py::test_first_input_advertises_metadata_then_reads_only_selected_body[outcomes-and-expertise-True]
ERROR tests/test_analysis_skills.py::test_skill_read_preserves_inline_compaction_pairing_and_canonical_prefix
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/../service.py-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/../service.py-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/../../../../README.md-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/../../../../README.md-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills\\..\\service.py-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills\\..\\service.py-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/.env-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/.env-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/README.md-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/README.md-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agent\\tests\\test_analysis_skills.py-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[S:\\caliburn\\.worktrees\\analysis-only-agent\\experiments\\analysis-agent\\tests\\test_analysis_skills.py-True]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/S:/caliburn/.worktrees/analysis-only-agent/experiments/analysis-agent/tests/test_analysis_skills.py-False]
ERROR tests/test_analysis_skills.py::test_read_file_rejects_traversal_and_host_paths[/skills/S:/caliburn/.worktrees/analysis-only-agent/experiments/analysis-agent/tests/test_analysis_skills.py-True]
ERROR tests/test_analysis_skills.py::test_invalid_routed_ls_directory_is_tool_feedback_not_service_interruption[False]
ERROR tests/test_analysis_skills.py::test_invalid_routed_ls_directory_is_tool_feedback_not_service_interruption[True]
ERROR tests/test_analysis_skills.py::test_shared_ls_grep_and_read_use_the_same_advertised_mount
ERROR tests/test_analysis_skills.py::test_pending_skill_read_uses_existing_trusted_read_recovery[False]
ERROR tests/test_analysis_skills.py::test_pending_skill_read_uses_existing_trusted_read_recovery[True]
ERROR tests/test_analysis_skills.py::test_skill_tool_body_stays_canonical_but_never_enters_b1_source_or_wire
ERROR tests/test_api.py::test_api_lifespan_idempotency_and_private_state_projection
ERROR tests/test_api.py::test_api_cross_document_access_and_empty_input - Per...
ERROR tests/test_api.py::test_api_background_status_is_safe_and_not_a_manual_consolidation_entry
ERROR tests/test_context_budget.py::test_a_each_call_includes_skills_memory_guide_results_and_preserves_canonical[False]
ERROR tests/test_context_budget.py::test_a_each_call_includes_skills_memory_guide_results_and_preserves_canonical[True]
ERROR tests/test_context_budget.py::test_a_budget_error_closes_unlocks_and_is_not_resumable[False]
ERROR tests/test_context_budget.py::test_a_budget_error_closes_unlocks_and_is_not_resumable[True]
ERROR tests/test_context_budget.py::test_b1_schema_and_b2_read_loop_actual_payload[False]
ERROR tests/test_context_budget.py::test_b1_schema_and_b2_read_loop_actual_payload[True]
ERROR tests/test_context_budget.py::test_background_budget_blocks_without_repeat_per_tick[b1]
ERROR tests/test_context_budget.py::test_background_budget_blocks_without_repeat_per_tick[b2]
ERROR tests/test_context_budget.py::test_api_surfaces_safe_counter_failure_without_generation[overflow-context_budget_exceeded-configuration_error-False]
ERROR tests/test_context_budget.py::test_api_surfaces_safe_counter_failure_without_generation[schema-configuration_error-configuration_error-False]
ERROR tests/test_context_budget.py::test_api_surfaces_safe_counter_failure_without_generation[utf8-configuration_error-configuration_error-False]
ERROR tests/test_context_budget.py::test_api_surfaces_safe_counter_failure_without_generation[timeout-transport_error-interrupted-True]
ERROR tests/test_extraction_role.py::test_service_background_selects_high_only_for_extraction[1000-False]
ERROR tests/test_extraction_role.py::test_service_background_selects_high_only_for_extraction[1000-True]
ERROR tests/test_extraction_role.py::test_service_background_selects_high_only_for_extraction[6000-False]
ERROR tests/test_extraction_role.py::test_service_background_selects_high_only_for_extraction[6000-True]
ERROR tests/test_extraction_role.py::test_real_factory_routes_models_through_same_budget_and_lifetime[None-None]
ERROR tests/test_extraction_role.py::test_real_factory_routes_models_through_same_budget_and_lifetime[high-None]
ERROR tests/test_extraction_role.py::test_real_factory_routes_models_through_same_budget_and_lifetime[medium-high]
ERROR tests/test_extraction_role.py::test_real_factory_routes_models_through_same_budget_and_lifetime[medium-medium]
ERROR tests/test_jd_admission.py::test_root_only_manual_binding_preserves_messages_and_first_true_input[False]
ERROR tests/test_jd_admission.py::test_root_only_manual_binding_preserves_messages_and_first_true_input[True]
ERROR tests/test_jd_api.py::test_create_identity_metadata_and_ui_filter_do_not_filter_scheduler
ERROR tests/test_jd_api.py::test_two_web_origins_and_preflight_only - Permiss...
ERROR tests/test_jd_api.py::test_run_lookup_is_read_only_and_digest_covers_abandon_pending
ERROR tests/test_jd_fix1_admission.py::test_r01_archive_between_admission_sections_cannot_save_input
ERROR tests/test_jd_fix1_admission.py::test_r04_foreground_closure_does_not_hold_other_document[stop]
ERROR tests/test_jd_fix1_admission.py::test_r04_foreground_closure_does_not_hold_other_document[abandon]
ERROR tests/test_jd_skill_contract.py::test_sdk_discovers_method_then_reads_only_requested_asset[False]
ERROR tests/test_jd_skill_contract.py::test_sdk_discovers_method_then_reads_only_requested_asset[True]
ERROR tests/test_jd_stop_projection.py::test_close_waits_for_stop_final_projection[False]
ERROR tests/test_jd_stop_projection.py::test_close_waits_for_stop_final_projection[True]
ERROR tests/test_memory_prompt_contract.py::test_service_initial_memory_request_matches_reviewed_contract
ERROR tests/test_memory_prompt_contract.py::test_service_default_budget_can_complete_multisource_read
ERROR tests/test_native_context_budget.py::test_provider_overflow_closes_turn_without_erasing_source_or_locking_next_input
ERROR tests/test_native_context_budget.py::test_native_api_two_turns_replay_reasoning_and_report_actual_usage
ERROR tests/test_postgres_scheduling.py::test_pg_background_rebuild_resumes_exact_saved_stage[notification_only-2]
ERROR tests/test_postgres_scheduling.py::test_pg_background_rebuild_resumes_exact_saved_stage[b1_saved-1]
ERROR tests/test_postgres_scheduling.py::test_pg_background_rebuild_resumes_exact_saved_stage[b2_tool_saved-1]
ERROR tests/test_postgres_scheduling.py::test_pg_background_rebuild_resumes_exact_saved_stage[memory_prepared-0]
ERROR tests/test_postgres_scheduling.py::test_pg_background_rebuild_resumes_exact_saved_stage[publication_reply-0]
ERROR tests/test_postgres_scheduling.py::test_pg_blocked_error_remains_blocked_after_rebuilding_all_clients
ERROR tests/test_postgres_service.py::test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[saved_input]
ERROR tests/test_postgres_service.py::test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[saved_result]
ERROR tests/test_postgres_service.py::test_pg_catalog_and_checkpoint_reconcile_across_rebuilt_clients[transport_failure]
ERROR tests/test_postgres_service.py::test_pg_main_service_reads_document_memory_and_keeps_native_items
ERROR tests/test_postgres_service.py::test_pg_service_rebuild_validates_and_reads_memory_source_links
ERROR tests/test_postgres_service.py::test_pg_uncertain_repair_can_explicitly_resume_same_operation[False]
ERROR tests/test_postgres_service.py::test_pg_uncertain_repair_can_explicitly_resume_same_operation[True]
ERROR tests/test_postgres_service.py::test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[None]
ERROR tests/test_postgres_service.py::test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[native]
ERROR tests/test_postgres_service.py::test_pg_default_api_lifespan_owns_real_clients_and_configures_provider[exact]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[read_conversation-False]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[read_conversation-True]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[read_file-False]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[read_file-True]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[ls-False]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[ls-True]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[grep-False]
ERROR tests/test_read_recovery.py::test_pending_trusted_read_resumes_original_call_without_prior_replay[grep-True]
ERROR tests/test_read_recovery.py::test_read_resume_at_model_limit_finishes_tool_without_resetting_budget[False]
ERROR tests/test_read_recovery.py::test_read_resume_at_model_limit_finishes_tool_without_resetting_budget[True]
ERROR tests/test_read_recovery.py::test_read_closure_pairs_unavailable_result_and_allows_new_input[stop-False]
ERROR tests/test_read_recovery.py::test_read_closure_pairs_unavailable_result_and_allows_new_input[stop-True]
ERROR tests/test_read_recovery.py::test_read_closure_pairs_unavailable_result_and_allows_new_input[abandon-False]
ERROR tests/test_read_recovery.py::test_read_closure_pairs_unavailable_result_and_allows_new_input[abandon-True]
ERROR tests/test_read_recovery.py::test_unknown_entered_tool_stays_blocked[runtime_error-False]
ERROR tests/test_read_recovery.py::test_unknown_entered_tool_stays_blocked[runtime_error-True]
ERROR tests/test_read_recovery.py::test_unknown_entered_tool_stays_blocked[process_interrupted-False]
ERROR tests/test_read_recovery.py::test_unknown_entered_tool_stays_blocked[process_interrupted-True]
ERROR tests/test_read_recovery.py::test_same_name_tool_cannot_inherit_memory_read_capability[read_conversation]
ERROR tests/test_read_recovery.py::test_same_name_tool_cannot_inherit_memory_read_capability[read_file]
ERROR tests/test_read_recovery.py::test_same_name_tool_cannot_inherit_memory_read_capability[ls]
ERROR tests/test_read_recovery.py::test_same_name_tool_cannot_inherit_memory_read_capability[grep]
ERROR tests/test_scheduling.py::test_notification_runs_real_b_without_repeating_published_source
ERROR tests/test_scheduling.py::test_background_wire_preserves_configured_output_limit[1000]
ERROR tests/test_scheduling.py::test_background_wire_preserves_configured_output_limit[6000]
ERROR tests/test_scheduling.py::test_unnotified_text_stays_saved_until_explicit_fallback_threshold
ERROR tests/test_scheduling.py::test_failed_b_stays_blocked_across_ticks_new_input_and_rebuild
ERROR tests/test_scheduling.py::test_b_is_independent_and_only_one_can_run_globally
ERROR tests/test_scheduling.py::test_partial_batch_keeps_target_after_its_first_notification_is_consumed
ERROR tests/test_scheduling.py::test_rebuild_resumes_saved_b1_without_another_extraction
ERROR tests/test_scheduling.py::test_repeated_process_loss_cannot_reset_recovery_budget
ERROR tests/test_scheduling.py::test_commit_reply_loss_reconciles_without_model_or_stale_failure_notice
ERROR tests/test_scheduling.py::test_current_failure_is_request_context_not_conversation_and_clears_on_recovery
ERROR tests/test_scheduling.py::test_scheduler_wakes_real_background_and_service_close_joins_it
ERROR tests/test_scheduling.py::test_blocked_document_does_not_starve_other_documents
ERROR tests/test_scheduling.py::test_pending_foreground_is_not_extracted_and_closed_failure_is_not_lost
ERROR tests/test_scheduling.py::test_dispatcher_cannot_start_model_after_service_shutdown
ERROR tests/test_service.py::test_accepted_input_is_saved_and_double_submit_does_not_invoke_twice
ERROR tests/test_service.py::test_service_default_does_not_cut_off_multistep_work_before_final
ERROR tests/test_service.py::test_stop_waits_for_provider_and_does_not_execute_returned_tool
ERROR tests/test_service.py::test_reopen_lost_api_reply_reconciles_checkpoint_without_paid_restart
ERROR tests/test_service.py::test_transient_failure_resumes_same_input_and_persists_resume_count
ERROR tests/test_service.py::test_configuration_failure_seals_turn_and_new_input_is_allowed
ERROR tests/test_service.py::test_failed_input_save_can_retry_same_key_without_being_stuck
ERROR tests/test_service.py::test_saved_input_without_worker_is_recovered_not_appended_twice
ERROR tests/test_service.py::test_explicit_abandon_preserves_original_employee_message
ERROR tests/test_service.py::test_shutdown_joins_inflight_provider_before_client_can_close
ERROR tests/test_service.py::test_stop_saved_input_before_child_exists_and_restart_remembers_stop
ERROR tests/test_service.py::test_completed_usage_is_reported_from_real_provider_metadata
ERROR tests/test_service.py::test_stop_after_transport_failure_does_not_offer_unwanted_resume
ERROR tests/test_service.py::test_unsaved_old_request_cannot_acknowledge_or_stop_another_live_run[repeat]
ERROR tests/test_service.py::test_unsaved_old_request_cannot_acknowledge_or_stop_another_live_run[stop]
ERROR tests/test_service.py::test_exhausted_tool_quota_still_allows_resuming_final_model_answer
ERROR tests/test_service.py::test_stop_after_model_hooks_prevents_tool_entry
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[credit_balance_exhausted]
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[organization_spend_limit_exceeded]
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[project_spend_limit_exceeded]
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[organization_usage_limit_exceeded]
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[insufficient_quota]
ERROR tests/test_service.py::test_billing_errors_do_not_offer_manual_resume[None]
FAILED tests/test_windows_lifecycle.py::test_actual_bootstrap_mutex_competitor_cannot_terminate_live_api
FAILED tests/test_windows_lifecycle.py::test_abandoned_main_thread_still_requires_product_to_stop_exact_old_job
FAILED tests/test_windows_lifecycle.py::test_external_old_job_handle_prevents_reuse_until_a_new_explicit_start
FAILED tests/test_windows_lifecycle.py::test_new_bootstrap_crash_during_old_job_stop_never_claims_app_admission
4 failed, 494 passed, 131 skipped, 143 warnings, 142 errors in 49.73s
