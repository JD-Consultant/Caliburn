# Native JD profile

固定官方 Plate profile、七個 command、完整 candidate validation、read-only selection、同文件 UI copy helper。三個固定 bridge entry 的 wire types 全從 contract 生成，schema.ts 補同版 ID／links 與 span 一致性，不讀 DB 或來源。詳細限制／驗證見上層 README。
