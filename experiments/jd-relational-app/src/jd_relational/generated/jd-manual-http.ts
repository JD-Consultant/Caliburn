/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualOperationId".
 */
export type ManualOperationId = string;
/**
 * Exactly one named business effect; no generic batch or arbitrary database operation is accepted.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualCommand".
 */
export type ManualCommand =
  | ManualCreateTaskCommand
  | ManualReviseWorkCommand
  | ManualSetTextCommand
  | ManualInsertItemCommand
  | ManualDeleteItemCommand
  | ManualMoveItemCommand
  | ManualSetTaskCapabilityCommand
  | ManualReplaceSelectionCommand;
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualDocumentState".
 */
export type ManualDocumentState = ManualWritableState | ManualBlockedState;
/**
 * Presence and original receipt evidence must agree; no receipt is invented by a successful HTTP lookup.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualOperationState".
 */
export type ManualOperationState = ManualOperationNotFound | ManualOperationPending | ManualOperationObserved;
/**
 * Only an original bound operation whose durable receipt is still unconfirmed.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualPendingResult".
 */
export type ManualPendingResult =
  | UnconfirmedInvalidInput
  | UnconfirmedTargetMissing
  | UnconfirmedStaleView
  | UnconfirmedRelationshipConflict
  | UnconfirmedDependentItems
  | UnconfirmedSaveFailed
  | UnknownOutcomeResult;
/**
 * Only the original confirmed receipt; unbound request rejection is not a found operation.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualObservedResult".
 */
export type ManualObservedResult =
  | CommittedResult
  | NoChangeResult
  | ConfirmedInvalidInput
  | ConfirmedTargetMissing
  | ConfirmedStaleView
  | ConfirmedRelationshipConflict
  | ConfirmedDependentItems
  | ConfirmedSaveFailed;

/**
 * Generation catalog for the manual HTTP seam. Endpoints exchange only their named definitions; commands and write observations retain their own authoritative schemas.
 */
export interface ManualHttpCatalog {
  ManualSaveInput: ManualSaveInput;
  ManualDocumentState: ManualDocumentState;
  ManualOperationState: ManualOperationState;
  ManualProblem: ManualProblem;
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualSaveInput".
 */
export interface ManualSaveInput {
  operation_id: ManualOperationId;
  base_revision_ref: string;
  command: ManualCommand;
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualCreateTaskCommand".
 */
export interface ManualCreateTaskCommand {
  tool: "jd_create_task";
  arguments: CreateTaskInput;
}
/**
 * Create one identifiable task with all currently known outcomes, requirements and existing capability relations as one business effect. The App allocates identities and validates the final task before saving.
 */
export interface CreateTaskInput {
  /**
   * Issued duty or unassigned-task container reference.
   */
  container_ref: string;
  /**
   * Issued existing sibling reference; null places the task first.
   */
  after_ref: string | null;
  /**
   * Known task name. At least name or description must contain meaningful content.
   */
  name: string | null;
  /**
   * Complete known task description and necessary task-specific scope; unknown content is not invented.
   */
  description: string | null;
  /**
   * Issued sources for the task itself, not automatically for its children or capability relations.
   */
  basis_refs: string[];
  outcomes: TaskDetailInput[];
  requirements: TaskDetailInput[];
  capabilities: TaskCapabilityInput[];
}
export interface TaskDetailInput {
  /**
   * The complete outcome or execution requirement, supported by the interview.
   */
  text: string;
  /**
   * Issued source references supporting only this item; empty when none are available.
   */
  basis_refs: string[];
}
export interface TaskCapabilityInput {
  /**
   * Issued reference to existing knowledge or skill in this JD.
   */
  capability_ref: string;
  /**
   * Issued sources supporting this task-to-capability relation.
   */
  basis_refs: string[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualReviseWorkCommand".
 */
export interface ManualReviseWorkCommand {
  tool: "jd_revise_work";
  arguments: ReviseWorkInput;
}
/**
 * Apply one complete correction using only the six named changes, all against the same current view. The shared domain rejects the whole correction on any invalid target, conflicting change or limit failure; it does not save partial changes.
 */
export interface ReviseWorkInput {
  /**
   * At least one change. The App validates configured limits, final content, references and relations before saving.
   */
  changes: (
    | SetFieldChange
    | AddTaskDetailChange
    | RemoveTaskDetailChange
    | SetTaskCapabilityChange
    | AddConditionChange
    | RemoveConditionChange
  )[];
}
export interface SetFieldChange {
  kind: "set_field";
  /**
   * Issued reference to one existing full-text field, never a selection or arbitrary JSON path.
   */
  target_field_ref: string;
  /**
   * The complete replacement field value. Null is allowed only when the final domain item permits it.
   */
  text: string | null;
  basis_refs: string[];
}
export interface AddTaskDetailChange {
  kind: "add_task_detail";
  task_ref: string;
  detail_kind: "outcome" | "requirement";
  /**
   * Issued surviving sibling of the same detail kind; null means first.
   */
  after_ref: string | null;
  text: string;
  basis_refs: string[];
}
export interface RemoveTaskDetailChange {
  kind: "remove_task_detail";
  /**
   * Issued reference to an existing outcome or requirement.
   */
  detail_ref: string;
}
export interface SetTaskCapabilityChange {
  kind: "set_task_capability";
  task_ref: string;
  capability_ref: string;
  mode: "link" | "unlink";
  /**
   * Issued sources for linking. Must be empty when unlinking; the shared domain validates this condition.
   */
  basis_refs: string[];
}
export interface AddConditionChange {
  kind: "add_condition";
  /**
   * Issued sixth-section container for an existing condition kind.
   */
  container_ref: string;
  after_ref: string | null;
  text: string;
  basis_refs: string[];
}
export interface RemoveConditionChange {
  kind: "remove_condition";
  condition_ref: string;
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualSetTextCommand".
 */
export interface ManualSetTextCommand {
  tool: "jd_set_text";
  arguments: SetTextInput;
}
/**
 * Replace one existing field with its complete text. This does not accept a selection reference. Related multi-field corrections use revise_work; the domain validates meaningful final content.
 */
export interface SetTextInput {
  /**
   * Issued reference to an existing full-text field, never an item, selection or JSON path.
   */
  target_field_ref: string;
  /**
   * Complete replacement value. Null is accepted only if the target field and final item allow it; clearing a field does not delete the item.
   */
  text: string | null;
  /**
   * Issued sources checked against the resulting content. Empty preserves existing links and their previous basis.
   */
  basis_refs: string[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualInsertItemCommand".
 */
export interface ManualInsertItemCommand {
  tool: "jd_insert_item";
  arguments: InsertItemInput;
}
/**
 * Create one duty, collaborator, shared knowledge or skill, task outcome or requirement, or position-wide condition with its currently known content. Create complete tasks with create_task, never as item shells. The domain validates each final item and allocates its identity.
 */
export interface InsertItemInput {
  item:
    | DutyItemInput
    | CollaboratorItemInput
    | KnowledgeItemInput
    | SkillItemInput
    | OutcomeItemInput
    | RequirementItemInput
    | ConditionItemInput;
}
export interface DutyItemInput {
  kind: "duty";
  /**
   * Issued duty-list container.
   */
  container_ref: string;
  /**
   * Issued existing sibling or null for first.
   */
  after_ref: string | null;
  name: string | null;
  /**
   * Known duty summary. Necessary task-specific limits remain on the tasks; no implicit condition inheritance.
   */
  scope_text: string | null;
  basis_refs: string[];
}
export interface CollaboratorItemInput {
  kind: "collaborator";
  /**
   * Issued collaborator-list container.
   */
  container_ref: string;
  /**
   * Issued existing sibling or null for first.
   */
  after_ref: string | null;
  name: string | null;
  /**
   * Known cooperation scope; unknown organizational roles are not invented.
   */
  scope_text: string | null;
  basis_refs: string[];
}
export interface KnowledgeItemInput {
  kind: "knowledge";
  /**
   * Issued knowledge-list container.
   */
  container_ref: string;
  /**
   * Issued existing sibling or null for first.
   */
  after_ref: string | null;
  name: string | null;
  /**
   * Known concepts, principles or rules needed for the work, including their use and scope. Names need not be unique.
   */
  description: string | null;
  basis_refs: string[];
}
export interface SkillItemInput {
  kind: "skill";
  /**
   * Issued skill-list container.
   */
  container_ref: string;
  /**
   * Issued existing sibling or null for first.
   */
  after_ref: string | null;
  name: string | null;
  /**
   * Known methods applied to perform the work and their scope, not an employee score or training plan.
   */
  description: string | null;
  basis_refs: string[];
}
export interface OutcomeItemInput {
  kind: "outcome";
  /**
   * Issued outcome-list container of an existing task.
   */
  container_ref: string;
  /**
   * Issued existing outcome sibling or null for first.
   */
  after_ref: string | null;
  /**
   * Known deliverable, service result or maintained state and its receiver where relevant. No requirement pairing is implied.
   */
  text: string;
  basis_refs: string[];
}
export interface RequirementItemInput {
  kind: "requirement";
  /**
   * Issued requirement-list container of an existing task.
   */
  container_ref: string;
  /**
   * Issued existing requirement sibling or null for first.
   */
  after_ref: string | null;
  /**
   * Known execution, quality, safety, timing or exception requirement, with its applicable condition. Kept separate from task description and outcomes.
   */
  text: string;
  basis_refs: string[];
}
export interface ConditionItemInput {
  kind: "condition";
  /**
   * Issued sixth-section container identifying the existing condition kind. Only position-wide conditions belong here.
   */
  container_ref: string;
  /**
   * Issued existing sibling in that condition kind or null for first.
   */
  after_ref: string | null;
  /**
   * Known whole-position condition or boundary. Task-specific scope stays on the task; unknown does not mean no requirement.
   */
  text: string;
  basis_refs: string[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualDeleteItemCommand".
 */
export interface ManualDeleteItemCommand {
  tool: "jd_delete_item";
  arguments: DeleteItemInput;
}
/**
 * Delete one existing item with its defined ownership effects. Deleting a duty retains its tasks unassigned, including necessary scope; deleting linked shared knowledge or skill is rejected. Only duty deletion may include the two bounded content adjustments for its surviving tasks, validated together by the domain.
 */
export interface DeleteItemInput {
  /**
   * Issued reference to the existing item; not a field or selection.
   */
  target_ref: string;
  /**
   * Empty for ordinary deletion. Duty deletion may adjust related surviving task fields or add outcomes/requirements so necessary scope is not lost. No unrelated changes or references to newly created items.
   */
  content_changes: (SetFieldChange | AddTaskDetailChange)[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualMoveItemCommand".
 */
export interface ManualMoveItemCommand {
  tool: "jd_move_item";
  arguments: MoveItemInput;
}
/**
 * Move a task between duties or the unassigned list, or reorder an existing item. Preserve identity, owned content, capability relations and sources. Grouping does not inherit destination conditions. The domain validates permitted related scope adjustments and the structural change as one complete candidate.
 */
export interface MoveItemInput {
  /**
   * Issued reference to the existing item, never a newly allocated or reconstructed item.
   */
  target_ref: string;
  /**
   * Issued compatible destination container. The App resolves ownership and condition kind.
   */
  destination_container_ref: string;
  /**
   * Issued existing sibling in the destination container; null means first.
   */
  after_ref: string | null;
  /**
   * Empty for ordinary reorder. Task regrouping may adjust its fields, add its outcomes/requirements, or update source/destination duty summaries. Only the related targets allowed by the domain may change.
   */
  content_changes: (SetFieldChange | AddTaskDetailChange)[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualSetTaskCapabilityCommand".
 */
export interface ManualSetTaskCapabilityCommand {
  tool: "jd_set_task_capability";
  arguments: SetTaskCapabilityInput;
}
/**
 * Link or unlink one task and existing shared knowledge or skill using issued references. Names are not identities. Unlinking does not delete the shared definition or change other tasks; reverse uses come from the same relation.
 */
export interface SetTaskCapabilityInput {
  task_ref: string;
  /**
   * Issued reference to existing knowledge or skill in this JD, not a name or copied definition.
   */
  capability_ref: string;
  mode: "link" | "unlink";
  /**
   * Issued sources for linking. Must be empty for unlink; the shared domain validates that condition.
   */
  basis_refs: string[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualReplaceSelectionCommand".
 */
export interface ManualReplaceSelectionCommand {
  tool: "jd_replace_selection";
  arguments: ReplaceSelectionInput;
}
/**
 * Replace exactly the text identified by an App-issued selection in one saved field. This input is replacement text only, never the whole field. The App owns the base, field, exact range and Unicode offset validation; stale or mismatched selections are rejected without searching for matching text.
 */
export interface ReplaceSelectionInput {
  /**
   * Issued selection reference only. Do not provide a field reference, old text, line numbers or offsets.
   */
  selection_ref: string;
  /**
   * Replacement for only the selected span, including any intended line breaks. Empty string removes the span; the domain validates the resulting field and item.
   */
  replacement_text: string;
  /**
   * Sources checked against the complete resulting source target. Leave empty when only the selected fragment was checked; do not promote fragment support to whole-target support.
   */
  basis_refs: string[];
}
/**
 * An unblocked document must be ready, active and idle with no pending operation or error.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualWritableState".
 */
export interface ManualWritableState {
  ready: true;
  archived: false;
  write_blocked: false;
  running: false;
  operation_id: null;
  error: null;
}
/**
 * Conservatively blocked while archived, busy, unavailable or awaiting recovery; no implication of write success or rollback.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualBlockedState".
 */
export interface ManualBlockedState {
  ready: boolean;
  archived: boolean;
  write_blocked: true;
  running: boolean;
  operation_id: ManualOperationId | null;
  error: "busy" | "recovery_required" | "service_unavailable" | null;
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualOperationNotFound".
 */
export interface ManualOperationNotFound {
  operation_id: ManualOperationId;
  presence: "not_found";
  result: null;
  write_state: ManualDocumentState;
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualOperationPending".
 */
export interface ManualOperationPending {
  operation_id: ManualOperationId;
  presence: "pending";
  result: ManualPendingResult;
  write_state: ManualDocumentState;
}
export interface UnconfirmedInvalidInput {
  status: "invalid_input";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: InvalidInputError;
  next_action: "reconcile_operation";
}
export interface InvalidInputError {
  code: "invalid_input";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnconfirmedTargetMissing {
  status: "target_missing";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: TargetMissingError;
  next_action: "reconcile_operation";
}
export interface TargetMissingError {
  code: "target_missing";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnconfirmedStaleView {
  status: "stale_view";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: StaleViewError;
  next_action: "reconcile_operation";
}
export interface StaleViewError {
  code: "stale_view";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnconfirmedRelationshipConflict {
  status: "relationship_conflict";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: RelationshipConflictError;
  next_action: "reconcile_operation";
}
export interface RelationshipConflictError {
  code: "relationship_conflict";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnconfirmedDependentItems {
  status: "dependent_items";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: DependentItemsError;
  next_action: "reconcile_operation";
}
export interface DependentItemsError {
  code: "dependent_items";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnconfirmedSaveFailed {
  status: "save_failed";
  effect: "unchanged";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: SaveFailedError;
  next_action: "reconcile_operation";
}
export interface SaveFailedError {
  code: "save_failed";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
export interface UnknownOutcomeResult {
  status: "outcome_unknown";
  effect: "unknown";
  receipt_durability: "unconfirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: OutcomeUnknownError;
  next_action: "reconcile_operation";
}
export interface OutcomeUnknownError {
  code: "outcome_unknown";
  message: string;
  /**
   * @maxItems 64
   */
  related_refs: string[];
}
/**
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualOperationObserved".
 */
export interface ManualOperationObserved {
  operation_id: ManualOperationId;
  presence: "observed";
  result: ManualObservedResult;
  write_state: ManualDocumentState;
}
export interface CommittedResult {
  status: "committed";
  effect: "changed";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: string;
  change_ref: string;
  error: null;
  next_action: "continue";
}
export interface NoChangeResult {
  status: "no_change";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: string;
  change_ref: null;
  error: null;
  next_action: "continue";
}
export interface ConfirmedInvalidInput {
  status: "invalid_input";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: InvalidInputError;
  next_action: "correct_arguments" | "stop";
}
export interface ConfirmedTargetMissing {
  status: "target_missing";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: TargetMissingError;
  next_action: "reread_current" | "stop";
}
export interface ConfirmedStaleView {
  status: "stale_view";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: StaleViewError;
  next_action: "reread_current" | "stop";
}
export interface ConfirmedRelationshipConflict {
  status: "relationship_conflict";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: RelationshipConflictError;
  next_action: "correct_arguments" | "stop";
}
export interface ConfirmedDependentItems {
  status: "dependent_items";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: DependentItemsError;
  next_action: "resolve_dependencies" | "stop";
}
export interface ConfirmedSaveFailed {
  status: "save_failed";
  effect: "unchanged";
  receipt_durability: "confirmed";
  operation_ref: string;
  result_revision_ref: null;
  change_ref: null;
  error: SaveFailedError;
  next_action: "stop";
}
/**
 * RFC 9457 request failure at the App layer. This problem does not establish the original mutation outcome, durability or rollback.
 *
 * This interface was referenced by `ManualHttpCatalog`'s JSON-Schema
 * via the `definition` "ManualProblem".
 */
export interface ManualProblem {
  type: "about:blank";
  title: string;
  status: 403 | 404 | 409 | 422 | 500 | 503;
  detail: string;
  instance: string;
  code:
    | "invalid_input"
    | "invalid_ref"
    | "target_missing"
    | "stale_view"
    | "operation_conflict"
    | "busy"
    | "service_unavailable"
    | "selection_not_available"
    | "origin_not_allowed";
  next_action: "correct_input" | "reread" | "lookup_operation" | "wait" | "stop";
}
